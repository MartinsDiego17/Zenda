import { DashboardAdmin } from "@/app/core/dashboard/components/admin/DashboardAdmin";

export const metadata = {
    title: 'Zenda: Administrador',
}

export default function Dashboard() {
    return (
        <DashboardAdmin />
    )
}